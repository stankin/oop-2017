 $(document).ready(function () {   // запускаем процесс формирования страницы, назначаем обработчик на каждую кнопку с классом "mode", на событие click (нажатие) // сам режим храним в ID элемента и передаем на сервер, чтобы потом вернуть в теге "header" XML документа         $(".mode").click( function(){          transfer.getRequest(this.id);          //Добавим выделение меню простым и дебильным способом, для примера работы с селекторами))):          //Зачистим уже выделенные, если есть, так есть, нет так нет          $(".mode").removeClass("active_button");          //добавим класс элементу на который кликнул          $("#"+this.id).addClass("active_button");         });                  //загрузка файлов        $("form[name='uploader']").submit(function(e) {        var formData = new FormData($(this)[0]);        $.ajax({            url: 'upload.php',            type: "POST",            data: formData,            success: function (msg) {                alert(msg);                $('form')[0].reset();            },            error: function(msg) {                alert('Ошибка!');            },            cache: false,            contentType: false,            processData: false        });        e.preventDefault();    }); });
